name: Node CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install 🔧
        run: npm install

      - name: Build 🏗️
        run: npm run build
